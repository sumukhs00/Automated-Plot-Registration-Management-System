/*jslint browser:true */
'use strict';

window.erd = window.erd || {};

(function() {

  erd.Table = function(details) {
    erd.BaseTable.call(this, details);
  };

  _.assign(erd.Table.prototype, erd.BaseTable.prototype);

  erd.Table.prototype.getType = function() {
    return 'Table';
  };

})();
