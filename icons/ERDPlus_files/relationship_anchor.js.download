/*jslint browser:true */
'use strict';

window.erd = window.erd || {};

(function() {

  var WIDTH = 20;

  erd.RelationshipAnchor = function(owner, dx, dy, side) {
    erd.Anchor.call(this, owner, dx, dy);
    this.side = side;
  };

  _.assign(erd.RelationshipAnchor.prototype, erd.Anchor.prototype);

})();
