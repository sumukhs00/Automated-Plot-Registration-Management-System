/*jslint browser:true */
'use strict';

window.erd = window.erd || {};

(function() {

  erd.BaseMouseHandler = function(stage) {
    this.stage = stage;
  };

  erd.BaseMouseHandler.prototype.onMouseMove = function(evt, x, y) {
  };

  erd.BaseMouseHandler.prototype.onMouseDown = function(evt, x, y) {
  };

  erd.BaseMouseHandler.prototype.onMouseUp = function(evt, x, y) {
  };

  erd.BaseMouseHandler.prototype.drawSpecial = function(context) {
  };

})();
